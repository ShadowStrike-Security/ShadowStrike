MEMORY SAFETY ANALYSIS FOR PeNormalizePath
==========================================

BUFFER OVERFLOW CHECK
---------------------

Line 278: allocLen = ((ULONG)inputChars + 1) * sizeof(WCHAR);

Maximum inputChars = 520 (SHADOWSTRIKE_MAX_EXCLUSION_PATH_LENGTH)
allocLen = (520 + 1) * 2 = 1042 bytes

Loop at line 303-323:
    for (i = 0; i < inputChars; i++) {
        ...
        buffer[outIndex] = RtlUpcaseUnicodeChar(ch);
        prevCh = ch;
        outIndex++;
    }

Problem analysis:
- outIndex starts at 0 (line 301)
- Loop runs inputChars times (max 520)
- Each iteration may increment outIndex (line 322)
- When does outIndex NOT increment?
  * When ch == PATH_SEPARATOR && prevCh == PATH_SEPARATOR (line 316-317)
  * This collapses consecutive separators

Worst case: No consecutive separators, so outIndex increments every iteration
- After 520 iterations, outIndex = 520
- buffer[519] is the last valid write (buffer has 521 WCHARs: 0-520)
- Line 320: buffer[outIndex] where outIndex can be 0 to 519 during loop
- Line 332: buffer[outIndex] = L'\0' where outIndex can be 0 to 520

RESULT: NO BUFFER OVERFLOW
- Buffer allocated for (inputChars + 1) = 521 WCHARs
- Max outIndex after loop is inputChars = 520
- Line 332 writes to buffer[520] at most, which is valid

NULL TERMINATOR CHECK
----------------------

Line 332: buffer[outIndex] = L'\0';

This happens AFTER the loop and AFTER trailing separator stripping.
Maximum outIndex is 520, which is within bounds (buffer has 521 WCHARs).

RESULT: NULL TERMINATOR IS CORRECT


UNICODE_STRING LENGTH FIELDS
-----------------------------

Line 335: NormalizedPath->Length = outIndex * sizeof(WCHAR);
Line 336: NormalizedPath->MaximumLength = (USHORT)allocLen;

allocLen is ULONG but cast to USHORT. Is this safe?

Line 285 has C_ASSERT:
    C_ASSERT(((ULONG)(SHADOWSTRIKE_MAX_EXCLUSION_PATH_LENGTH) + 1) * sizeof(WCHAR) <= MAXUSHORT);

This ensures allocLen never exceeds MAXUSHORT (65535).
With SHADOWSTRIKE_MAX_EXCLUSION_PATH_LENGTH = 520:
    allocLen = 1042, which is << 65535

RESULT: CAST IS SAFE

Length vs MaximumLength:
- Length = outIndex * sizeof(WCHAR) ≤ inputChars * sizeof(WCHAR) ≤ 1040
- MaximumLength = allocLen = 1042
- Length ≤ MaximumLength ✓ CORRECT

MEMORY LEAK CHECK
-----------------

Success path: buffer is stored in NormalizedPath->Buffer, caller owns it. ✓

Error paths:
1. Line 254: return before allocation ✓
2. Line 258: return before allocation ✓
3. Line 269: return before allocation ✓
4. Line 294: allocation failed, nothing to free ✓

RESULT: NO MEMORY LEAKS


POOL TYPE CHECK
---------------

Line 287-290:
    buffer = (PWCHAR)ExAllocatePool2(
        POOL_FLAG_PAGED,
        allocLen,
        SHADOWSTRIKE_EXCLUSION_POOL_TAG
    );

POOL_FLAG_PAGED is correct for UNICODE_STRING buffers.
Function is marked PAGED_CODE() at line 249.
Comment at line 274-276 explains the rationale.

RESULT: POOL TYPE IS CORRECT


SUMMARY: PeNormalizePath HAS NO BUGS
=====================================
All checks pass. No buffer overflow, memory leak, or incorrect field issues.
